<Window x:Class="DSMS.PresetStudio.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DSMS Preset Studio update" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False">
    <Border Background="{DynamicResource AppBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1" CornerRadius="12" Padding="1">
        <Border.Effect><DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.55"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="52"/><RowDefinition/></Grid.RowDefinitions>
            <Border Background="{DynamicResource PanelAltBrush}" CornerRadius="11,11,0,0"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Margin="16,0,8,0">
                    <TextBlock Text="DSMS PRESET STUDIO" Foreground="{DynamicResource PrimaryBrush}"
                               FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center"
                            Width="34" Height="30" Padding="0" Click="Later_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="24">
                <TextBlock x:Name="PromptTitleText" Text="Update available" FontSize="22" FontWeight="Bold"/>
                <TextBlock x:Name="PromptDescriptionText" Margin="0,8,0,0" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}"/>

                <Border Margin="0,18,0,0" Padding="14" CornerRadius="8"
                        Background="{DynamicResource InputBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="CurrentLabelText" Text="INSTALLED" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="CurrentVersionText" Margin="0,4,0,0" FontSize="16" FontWeight="SemiBold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="AvailableLabelText" Text="AVAILABLE" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="AvailableVersionText" Margin="0,4,0,0" FontSize="16" FontWeight="SemiBold"
                                       Foreground="{DynamicResource UpdateReadyBrush}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <TextBlock x:Name="SecurityText" Margin="0,14,0,0" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                    <Button x:Name="LaterButton" Content="Later" Click="Later_Click" Margin="0,0,9,0"/>
                    <Button x:Name="UpdateButton" Content="Update now" Click="Update_Click" Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
